services:
  frontend:
    build:
      context: .

    container_name: gamer_challenges_front

    ports:
      - "3000:3000"

    volumes:
      - .:/app
      - /app/node_modules

    environment:
      NODE_ENV: development

    restart: unless-stopped
